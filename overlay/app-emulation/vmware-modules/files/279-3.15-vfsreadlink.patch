--- inode.c.orig	2014-08-06 13:28:35.000000000 +0400
+++ inode.c	2014-08-06 13:31:51.000000000 +0400
@@ -177,7 +177,11 @@
       return -EINVAL;
    }
 
-   return vfs_readlink(dentry, buffer, buflen, iinfo->name);
+#if LINUX_VERSION_CODE <= KERNEL_VERSION(3, 14, 99)
+   return vfs_readlink(dentry, buffer, buflen, fileName);
+#else
+   return readlink_copy(buffer, buflen, fileName);
+#endif
 }
 
 
